# =============================================================================
# PostgreSQL credentials.
# POSTGRES_PASSWORD is injected from the top-level .env — do NOT put the
# real password here. This file is safe to commit.
# =============================================================================
POSTGRES_DB=netbox
POSTGRES_USER=netbox
# POSTGRES_PASSWORD is set in .env and injected by docker-compose
